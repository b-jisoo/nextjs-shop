import Head from "next/head";
import React from "react";

type Props = {};

const Seo = (props: Props) => {
  return (
    <Head>
      <title>next-shop</title>
    </Head>
  );
};

export default Seo;
